# RallyTech Fidelity Failure Modes

- hierarchy collapse on mobile
- hero looks premium but brand presence is weak
- CTA exists but loses primary status
- layout order matches but spacing rhythm does not
- copy length breaks the intended scan path
- variant styling turns into a different page instead of a branded translation
